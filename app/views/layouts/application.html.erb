<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>moveandgroove</title>
    <%= stylesheet_link_tag "application", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
</head>
<body class="bg-white text-gray-800 font-sans">
    <header class="py-4 px-6 bg-pink-100 border-b-4">
        <% if user_signed_in? %>
            <%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'visited text-blue-600 hover:text-green-800 font-semibold' %>
            <%= link_to 'Edit Profile', edit_user_registration_path, class: 'visited hover:text-green-800 hover:text-lg font-semibold' + (current_page?(edit_user_registration_path) ? ' text-green-600' : '') %>
            <%= link_to "View Profile", user_info_path, class: 'visited hover:text-green-800 hover:text-lg font-semibold' + (current_page?(user_info_path) ? ' text-green-600' : '') %>
            <%= link_to "Home", root_path, class: 'visited hover:text-green-800 hover:text-lg font-semibold' + (current_page?(root_path) ? ' text-green-600' : '') %>
            <%= link_to "About", about_path, class: 'visited hover:text-green-800 hover:text-lg font-semibold' + (current_page?(about_path) ? ' text-green-600' : '') %>
        <% else %>
            <%= link_to 'Sign in', new_user_session_path, class: 'visited hover:text-green-800 hover:text-lg font-semibold' + (current_page?(new_user_session_path) ? ' text-green-600' : '') %>
        <% end %>
    </header>

    <%= yield %>

    <footer class="py-4 px-6 bg-green-100 border-t-4 border-green-500">
        <%= link_to 'Edit Profile', edit_user_registration_path, class: 'visited hover:text-green-800 hover:text-lg font-semibold' + (current_page?(edit_user_registration_path) ? ' text-green-600' : '') %>
        <%= link_to "View Profile", user_info_path, class: 'visited hover:text-green-800 hover:text-lg font-semibold' + (current_page?(user_info_path) ? ' text-green-600' : '') %>
        <%= link_to "Home", root_path, class: 'visited hover:text-green-800 hover:text-lg font-semibold' + (current_page?(root_path) ? ' text-green-600' : '') %>
        <%= link_to "About", about_path, class: 'visited hover:text-green-800 hover:text-lg font-semibold' + (current_page?(about_path) ? ' text-green-600' : '') %>
        <%= link_to "Media", media_path, class: 'visited hover:text-green-800 hover:text-lg font-semibold' + (current_page?(media_path) ? ' text-green-600' : '') %>
    </footer>
</body>
</html>
