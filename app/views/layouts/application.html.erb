<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>moveandgroove</title>
    <%= stylesheet_link_tag "application", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&family=Kavoon&display=swap" rel="stylesheet">
</head>
<body class='bg-white text-gray-700'>
    <header class='py-12 px-6 bg-blue-500'>
        <div class='flex-wrap'>
            <h1 class='py-6 kavoon-regular'>
                <%= link_to "Move and Groove", root_path %>
            </h1>
            <% if user_signed_in? %>
                <%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'text-red-500 font-semibold pr-8' %>
                <%= link_to 'Edit Profile', edit_user_registration_path, class: 'font-semibold pr-6' + (current_page?(edit_user_registration_path) ? ' text-green-600' : '') %>
                <%= link_to "View Profile", user_info_path, class: 'font-semibold pr-6' + (current_page?(user_info_path) ? ' text-green-600' : '') %>
                <%= link_to "Home", root_path, class: 'font-semibold pr-6' + (current_page?(root_path) ? ' text-green-600' : '') %>
                <%= link_to "About", about_path, class: 'font-semibold' + (current_page?(about_path) ? ' text-green-600' : '') %>
            <% else %>
                <%= link_to 'Sign in', new_user_session_path, class: 'visited' + (current_page?(new_user_session_path) ? ' text-green-600' : '') %>
            <% end %>
        </div>
    </header>

    <%= yield %>

    <footer class="py-8 px-6 bg-green-500">
        <div class="inline-flex">
            <% if user_signed_in? %>
                <%= link_to 'Edit Profile', edit_user_registration_path, class: 'font-semibold pr-6' + (current_page?(edit_user_registration_path) ? ' text-light-blue' : '') %>
                <%= link_to "View Profile", user_info_path, class: 'font-semibold pr-6' + (current_page?(user_info_path) ? ' text-light-blue' : '') %>
                <%= link_to "Home", root_path, class: 'font-semibold pr-6' + (current_page?(root_path) ? ' text-light-blue' : '') %>
                <%= link_to "About", about_path, class: 'font-semibold pr-6' + (current_page?(about_path) ? ' text-light-blue' : '') %>
                <%= link_to "Media", media_path, class: 'font-semibold pr-6' + (current_page?(media_path) ? ' text-light-blue' : '') %>
            <% else %>
                <%= link_to 'Sign in', new_user_session_path, class: 'hover:text-lg' + (current_page?(new_user_session_path) ? ' text-light-blue' : '') %>
                <%= link_to "About", about_path, class: 'font-semibold' + (current_page?(about_path) ? ' text-light-blue' : '') %>
                <%= link_to "Media", media_path, class: 'font-semibold' + (current_page?(media_path) ? ' text-light-blue' : '') %>
            <% end %>
        </div>
    </footer>
</body>
</html>
