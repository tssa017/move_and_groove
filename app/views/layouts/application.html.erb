<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>moveandgroove</title>
    <%= stylesheet_link_tag "application", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
</head>
<body class='bg-white text-gray-800'>
    <header class='py-12 px-6 bg-blue-500'>
        <div class='flex-wrap'>
            <h1 class='py-6 text-gray-700 font-semibold text-lg'>Move and Groove</h1>
            <% if user_signed_in? %>
                <%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'visited text-red-500 hover:text-green-800 font-semibold pr-8' %>
                <%= link_to 'Edit Profile', edit_user_registration_path, class: 'visited hover:text-green-800 font-semibold pr-6' + (current_page?(edit_user_registration_path) ? ' text-green-600' : '') %>
                <%= link_to "View Profile", user_info_path, class: 'visited hover:text-green-800 font-semibold pr-6' + (current_page?(user_info_path) ? ' text-green-600' : '') %>
                <%= link_to "Home", root_path, class: 'visited hover:text-green-800 font-semibold pr-6' + (current_page?(root_path) ? ' text-green-600' : '') %>
                <%= link_to "About", about_path, class: 'visited hover:text-green-800 font-semibold' + (current_page?(about_path) ? ' text-green-600' : '') %>
            <% else %>
                <%= link_to 'Sign in', new_user_session_path, class: 'visited hover:text-green-800 hover:text-lg' + (current_page?(new_user_session_path) ? ' text-green-600' : '') %>
            <% end %>
        </div>
    </header>

    <%= yield %>

    <footer class="py-8 px-6 bg-green-500 flex-wrap">
        <%= link_to 'Edit Profile', edit_user_registration_path, class: 'visited hover:text-green-800 font-semibold' + (current_page?(edit_user_registration_path) ? ' text-green-600' : '') %>
        <%= link_to "View Profile", user_info_path, class: 'visited hover:text-green-800 font-semibold' + (current_page?(user_info_path) ? ' text-green-600' : '') %>
        <%= link_to "Home", root_path, class: 'visited hover:text-green-800 font-semibold' + (current_page?(root_path) ? ' text-green-600' : '') %>
        <%= link_to "About", about_path, class: 'visited hover:text-green-800 font-semibold' + (current_page?(about_path) ? ' text-green-600' : '') %>
        <%= link_to "Media", media_path, class: 'visited hover:text-green-800 font-semibold' + (current_page?(media_path) ? ' text-green-600' : '') %>
        <img src="/apple-touch-icon.png" alt="Move and Groove logo">
    </footer>
</body>
</html>
